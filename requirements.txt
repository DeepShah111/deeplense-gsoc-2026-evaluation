# DeepLense GSoC 2026 Evaluation Pipeline Dependencies
#
# ── INSTALL ORDER MATTERS ─────────────────────────────────────────────────────
# numpy MUST be pinned first. lenstronomy and astropy both declare numpy as a
# transitive dependency and will silently upgrade it, causing the binary
# incompatibility error:
#   "numpy.dtype size changed, may indicate binary incompatibility"
# escnn was compiled against numpy 1.26.4 specifically — any other version breaks.
#
# Correct install sequence (used by all notebooks and CLI):
#   pip install "numpy==1.26.4" --force-reinstall
#   pip install -r requirements.txt
#   pip install "numpy==1.26.4" --force-reinstall   ← second pass catches overrides
# ─────────────────────────────────────────────────────────────────────────────

# Data Manipulation & Computation — PINNED FIRST, do not move this line
numpy==1.26.4

# Core Deep Learning
torch>=2.0.0
torchvision>=0.15.0

# Astrophysics & Physics Engines
lenstronomy>=1.11.0
astropy>=5.3.0

# Machine Learning Metrics & Preprocessing
scikit-learn>=1.3.0

# Data Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Image Processing & Utilities
Pillow>=9.5.0
tqdm>=4.65.0
pandas>=2.0.0

# Equivariant Networks
escnn>=0.2.2

# Experiment Tracking
wandb>=0.16.0

# Utilities
python-dotenv>=1.0.0

# Interactive Demo (Gradio app + GradCAM overlay)
gradio>=4.0.0
opencv-python-headless>=4.8.0